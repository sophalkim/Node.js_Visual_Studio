extends layout

block content
  h2 #{title}.
  h3 #{message}
  h1 Hey Phal
  ul#messages
  form(action='')
    input#m(autocomplete='off')
    button(class='btn btn-success') Send
  button(id='alertText', onclick='showAlert()') Alert
  script.
    function showAlert() {
      alert("You are a cool cat.");
    }
